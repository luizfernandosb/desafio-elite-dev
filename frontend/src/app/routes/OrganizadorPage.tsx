import { PlaceholderPage } from './PlaceholderPage'

// Painel do organizador (busca no TMDb, formulários de criação/gestão de evento,
// upload de imagem) entra na etapa 04 -- não é código que o visitante público baixa.
export default function OrganizadorPage() {
  return <PlaceholderPage title="Painel do organizador" etapa="etapa 04" />
}
