import { PlaceholderPage } from './PlaceholderPage'

// Stripe.js entra aqui na etapa 08 -- não deveria pesar o carrinho de quem só está
// navegando ou reservando assento.
export default function CheckoutPage() {
  return <PlaceholderPage title="Checkout" etapa="etapa 08" />
}
